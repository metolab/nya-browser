--- a/third_party/blink/renderer/modules/webgl/webgl_rendering_context_base.cc
+++ b/third_party/blink/renderer/modules/webgl/webgl_rendering_context_base.cc
@@ -24,6 +24,7 @@
  */
 
 #include "third_party/blink/renderer/modules/webgl/webgl_rendering_context_base.h"
+#include "third_party/blink/renderer/platform/nya/nya_farbling.h"
 
 #include <inttypes.h>
 
@@ -4192,8 +4193,13 @@ ScriptValue WebGLRenderingContextBase::getParameter(
     case WebGLDebugRendererInfo::kUnmaskedRendererWebgl:
       if (ExtensionEnabled(kWebGLDebugRendererInfoName)) {
-        return WebGLAny(script_state,
-                        String(ContextGL()->GetString(GL_RENDERER)));
+        const char* renderer = reinterpret_cast<const char*>(
+            ContextGL()->GetString(GL_RENDERER));
+        return WebGLAny(
+            script_state,
+            String(nya::MaybeSpoofUnmaskedRenderer(renderer ? renderer : "")
+                       .c_str()));
       }
       SynthesizeGLError(
           GL_INVALID_ENUM, "getParameter",
@@ -5346,8 +5352,11 @@ void WebGLRenderingContextBase::ReadPixelsHelper(GLint x,
     if (!binder.Succeeded()) {
       return;
     }
     ContextGL()->ReadPixels(x, y, width, height, format, type, data);
   }
+  if (data && buffer_size.IsValid()) {
+    nya::FarbleBytes(data, static_cast<size_t>(buffer_size.ValueOrDie()));
+  }
 }
 
 void WebGLRenderingContextBase::RenderbufferStorageImpl(
