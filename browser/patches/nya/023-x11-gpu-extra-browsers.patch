diff --git a/chrome/browser/ui/nya/nya_one_compositor.cc b/chrome/browser/ui/nya/nya_one_compositor.cc
--- a/chrome/browser/ui/nya/nya_one_compositor.cc
+++ b/chrome/browser/ui/nya/nya_one_compositor.cc
@@ -16,9 +16,14 @@
       switches::kNyaX11MultiDisplay);
 }
 
-bool IsExtraX11BrowserType(views::Widget::InitParams::Type type) {
-  return type == views::Widget::InitParams::TYPE_WINDOW ||
-         type == views::Widget::InitParams::TYPE_WINDOW_FRAMELESS;
+bool IsExtraDesktopBrowser(const views::Widget::InitParams& params) {
+  if (!OneCompositorEnabled()) {
+    return false;
+  }
+  if (params.name != "BrowserWidget") {
+    return false;
+  }
+  return FindDesktopHost(params.x11_display) != nullptr;
 }
 
 gfx::NativeView FindDesktopHost(const std::string& x11_display) {
@@ -63,7 +68,7 @@
   if (!OneCompositorEnabled()) {
     return false;
   }
-  if (IsExtraX11BrowserType(params.type)) {
+  if (params.name == "BrowserWidget") {
     return false;
   }
   return FindDesktopHost(params) != nullptr;
diff --git a/chrome/browser/ui/nya/nya_one_compositor.h b/chrome/browser/ui/nya/nya_one_compositor.h
--- a/chrome/browser/ui/nya/nya_one_compositor.h
+++ b/chrome/browser/ui/nya/nya_one_compositor.h
@@ -11,16 +11,17 @@
 
 namespace nya {
 
-// --nya-x11-multi-display: Xvfb + in-process GPU can present exactly one
-// EGL-readback AcceleratedWidget per X display. Menus/bubbles stay as
-// NativeWidgetAura layers on that compositor. Extra Browser windows
-// (window.open, Ctrl+N) are a second X11 window with software compositing
-// so Openbox/tint2 can see them without a second EGL pbuffer.
+// --nya-x11-multi-display: menus/bubbles stay NativeWidgetAura on the
+// session GPU compositor. Extra Browser windows (window.open, Ctrl+N)
+// are real X11 Desktop widgets so Openbox/tint2 can see them. Each
+// extra window gets its own EGL-readback present (named FBO, 013);
+// refusing a second view made Chrome fall back to MIT-SHM (freeze) or
+// software compositing (blank WebContents).
 bool OneCompositorEnabled();
 
-// True for Browser-style widgets that must be real X11 windows (not hosted
-// on the session compositor).
-bool IsExtraX11BrowserType(views::Widget::InitParams::Type type);
+// True when this InitParams is an extra Browser window that should be a
+// real X11 window (params.name is "BrowserWidget" after BrowserWidget::Init).
+bool IsExtraDesktopBrowser(const views::Widget::InitParams& params);
 
 // Desktop host already open on |x11_display|. Empty display prefers the
 // active host (so window.open from a sub-display stays on that compositor),
@@ -32,7 +33,7 @@
 gfx::NativeView FindDesktopHost(const views::Widget::InitParams& params);
 
 // True when this widget must not create a second Desktop AcceleratedWidget.
-// Extra Browser TYPE_WINDOW is excluded — those use software X11 instead.
+// Extra BrowserWidget windows are excluded — they are real X11 GPU windows.
 bool ShouldHostInExistingCompositor(const views::Widget::InitParams& params);
 
 }  // namespace nya
diff --git a/chrome/browser/ui/views/chrome_views_delegate_linux.cc b/chrome/browser/ui/views/chrome_views_delegate_linux.cc
--- a/chrome/browser/ui/views/chrome_views_delegate_linux.cc
+++ b/chrome/browser/ui/views/chrome_views_delegate_linux.cc
@@ -49,10 +49,10 @@
 NativeWidgetType GetNativeWidgetTypeForInitParams(
     const views::Widget::InitParams& params) {
   // One EGL-readback compositor per X display. Menus/bubbles stay on that
-  // compositor as NativeWidgetAura. Extra Browser TYPE_WINDOW is a second
-  // X11 window with software compositing (see CreateNativeWidget).
+  // compositor as NativeWidgetAura. Extra BrowserWidget windows are a
+  // second X11 window with GPU readback present (see CreateNativeWidget).
   if (nya::OneCompositorEnabled()) {
-    if (nya::IsExtraX11BrowserType(params.type)) {
+    if (nya::IsExtraDesktopBrowser(params)) {
       return NativeWidgetType::kDesktopNativeWidgetAura;
     }
     if (nya::ShouldHostInExistingCompositor(params) || params.parent ||
@@ -99,19 +99,15 @@
     views::Widget::InitParams* params,
     views::internal::NativeWidgetDelegate* delegate) {
   if (nya::OneCompositorEnabled()) {
-    if (nya::IsExtraX11BrowserType(params->type) &&
-        nya::FindDesktopHost(params->x11_display)) {
-      // Second Chrome window on this X display: real X11 + software
-      // compositor. Parenting it to the GPU host would hide it from
-      // Openbox/tint2 and break drag. A second EGL pbuffer freezes
-      // in-process GPU, so force software present instead.
+    if (nya::IsExtraDesktopBrowser(*params)) {
+      // Second Chrome window: real X11 so tint2/Openbox see it. GPU
+      // readback present (not software) so WebContents can paint.
       params->parent = nullptr;
       params->context = nullptr;
       params->child = false;
-      params->force_software_compositing = true;
       params->show_state = ui::mojom::WindowShowState::kNormal;
       params->use_accelerated_widget_override = true;
-      LOG(ERROR) << "nya-widget extra-browser desktop software display="
+      LOG(ERROR) << "nya-widget extra-browser desktop gpu display="
                  << params->x11_display
                  << " bounds=" << params->bounds.ToString();
     } else if (gfx::NativeView host = nya::FindDesktopHost(*params)) {
diff --git a/chrome/browser/ui/views/frame/browser_native_widget_aura_linux.cc b/chrome/browser/ui/views/frame/browser_native_widget_aura_linux.cc
--- a/chrome/browser/ui/views/frame/browser_native_widget_aura_linux.cc
+++ b/chrome/browser/ui/views/frame/browser_native_widget_aura_linux.cc
@@ -81,7 +81,7 @@
   if (nya::OneCompositorEnabled()) {
     gfx::NativeView host = nya::FindDesktopHost(params.x11_display);
     if (host) {
-      params.force_software_compositing = true;
+      params.force_software_compositing = false;
       params.show_state = ui::mojom::WindowShowState::kNormal;
       gfx::Rect hb = host->GetBoundsInScreen();
       if (!hb.IsEmpty()) {
@@ -93,7 +93,7 @@
                                   hb.y() + (hb.height() - fit_h) / 2, fit_w,
                                   fit_h);
       }
-      LOG(ERROR) << "nya-widget extra-browser GetWidgetParams software bounds="
+      LOG(ERROR) << "nya-widget extra-browser GetWidgetParams gpu bounds="
                  << params.bounds.ToString();
     }
   }
@@ -181,8 +181,8 @@
 BrowserNativeWidget* BrowserNativeWidgetFactory::Create(
     BrowserWidget* browser_widget,
     BrowserView* browser_view) {
-  // Extra Browser windows are real X11 DesktopNativeWidgetAura with software
-  // compositing (022). Do not host them as NativeWidgetAura on the GPU
-  // compositor — that hides them from tint2 and makes drag a no-op.
+  // Extra Browser windows are real X11 DesktopNativeWidgetAura with GPU
+  // readback present (023). Do not host them as NativeWidgetAura on the
+  // session compositor — that hides them from tint2 and makes drag a no-op.
   return new BrowserNativeWidgetAuraLinux(browser_widget, browser_view);
 }
diff --git a/ui/ozone/platform/x11/x11_readback_present_cache.cc b/ui/ozone/platform/x11/x11_readback_present_cache.cc
--- a/ui/ozone/platform/x11/x11_readback_present_cache.cc
+++ b/ui/ozone/platform/x11/x11_readback_present_cache.cc
@@ -65,11 +65,19 @@
   auto& map = GetMap();
   base::AutoLock lock(map.lock);
   auto it = map.egl_view_by_display.find(display);
-  if (it == map.egl_view_by_display.end() || it->second == widget) {
+  if (it != map.egl_view_by_display.end() && it->second != widget) {
+    // Extra Browser X11 windows need their own readback present. A second
+    // pbuffer used to stall; 013's named FBO keeps glReadPixels off FBO 0.
+    // Refusing here blanked WebContents (software compositor) or froze
+    // the session (MIT-SHM fallback).
+    LOG(ERROR) << "nya-present extra EGL readback widget="
+               << static_cast<uint32_t>(widget)
+               << " display already has widget="
+               << static_cast<uint32_t>(it->second);
+  } else {
     map.egl_view_by_display[display] = widget;
-    return true;
   }
-  return false;
+  return true;
 }
 
 void X11ReadbackPresentCache::ReleaseEglView(gfx::AcceleratedWidget widget) {
diff --git a/ui/ozone/platform/x11/x11_readback_present_cache.h b/ui/ozone/platform/x11/x11_readback_present_cache.h
--- a/ui/ozone/platform/x11/x11_readback_present_cache.h
+++ b/ui/ozone/platform/x11/x11_readback_present_cache.h
@@ -28,9 +28,9 @@
   static bool Lookup(gfx::AcceleratedWidget widget,
                      X11ReadbackPresentTarget* out);
   static bool Contains(gfx::AcceleratedWidget widget);
-  // Last-resort freeze tripwire: at most one EGL-readback view per X display.
-  // Extra widgets must be NativeWidgetAura (014). A second pbuffer on
-  // in-process GPU stalls present; refuse before creating it.
+  // Extra Browser X11 windows may also claim a readback view. Named FBO
+  // present (013) is what keeps two pbuffers from sharing FBO 0. Refusing
+  // a second view blanked WebContents or fell back to MIT-SHM (freeze).
   static bool TryClaimEglView(gfx::AcceleratedWidget widget);
   static void ReleaseEglView(gfx::AcceleratedWidget widget);
 };
